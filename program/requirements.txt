# shopping-agent/requirements.txt
# Install with: pip install -r requirements.txt

streamlit>=1.35.0
boto3>=1.34.0
mcp>=1.0.0          # Official MCP Python SDK (includes stdio_client, ClientSession)
python-dotenv>=1.0.0